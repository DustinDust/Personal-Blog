import { PostNode as Post } from '../../types/responses/GetPost';

const FeaturedPostCard: React.FC<{ posts: Post }> = (props) => {
  return <div>Featured Post</div>;
};

export default FeaturedPostCard;
